version: '3.8'

services:
  kedro-app:
    build: .
    environment:
      - KEDRO_ENV=local
      - AWS_REGION=us-east-1
    volumes:
      - .:/app
      - ~/.aws:/root/.aws:ro
    command: kedro run --pipeline data_processing